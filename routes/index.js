const authRouter = require('./authRouter');
const profileRouter = require('./profileRouter');
const userRouter = require('./usersRouter');

module.exports = { authRouter, profileRouter, userRouter };
